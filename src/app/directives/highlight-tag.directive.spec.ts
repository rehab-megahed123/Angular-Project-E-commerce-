import { HighlightTagDirective } from './highlight-tag.directive';

describe('HighlightTagDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTagDirective();
    expect(directive).toBeTruthy();
  });
});
